import React from 'react';
import logo from './logo.png';

function App() {
    return (
        <div>
            Hello react
            <img src={logo} alt={logo} style={{ width: `250px` }} />
        </div>
    );
}

export default App;